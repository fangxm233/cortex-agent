Please update me when files in this folder change.

The agent-server update channel. Arbitrated by `features/update-prompt`, which it never imports.

| filename | role | function |
|---|---|---|
| ServerUpdateDialog.tsx | view | Render server update dialog and update copy helpers |
| ServerUpdateDialog.test.tsx | test | Test server update visibility rules |
| useServerUpdate.ts | hook | Server self-update state and `serverUpdateVisible` |
