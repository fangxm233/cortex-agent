// 1c 线程 — mobile v3 screen slot (scheme-mobile.dc.html). Placeholder body; filled by its own pass.
import { MScreen, MC } from '@/mobile/ui/kit';

export function MThreadsScreen() {
  return (
    <MScreen label="1c 线程">
      <div style={{ padding: 16, color: MC.muted, fontSize: 13 }}>1c 线程</div>
    </MScreen>
  );
}
