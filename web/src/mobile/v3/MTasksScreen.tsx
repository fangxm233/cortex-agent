// 1d 任务 — mobile v3 screen slot (scheme-mobile.dc.html). Placeholder body; filled by its own pass.
import { MScreen, MC } from '@/mobile/ui/kit';

export function MTasksScreen() {
  return (
    <MScreen label="1d 任务">
      <div style={{ padding: 16, color: MC.muted, fontSize: 13 }}>1d 任务</div>
    </MScreen>
  );
}
