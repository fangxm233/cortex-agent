// 1a 会话列表 — mobile v3 screen slot (scheme-mobile.dc.html). Placeholder body; filled by its own pass.
import { MScreen, MC } from '@/mobile/ui/kit';

export function MSessionListScreen() {
  return (
    <MScreen label="1a 会话列表">
      <div style={{ padding: 16, color: MC.muted, fontSize: 13 }}>1a 会话列表</div>
    </MScreen>
  );
}
