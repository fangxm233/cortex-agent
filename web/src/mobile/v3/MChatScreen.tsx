// 1b 会话详情 — mobile v3 screen slot (scheme-mobile.dc.html). Placeholder body; filled by its own pass.
import { MScreen, MC } from '@/mobile/ui/kit';

export function MChatScreen() {
  return (
    <MScreen label="1b 会话详情">
      <div style={{ padding: 16, color: MC.muted, fontSize: 13 }}>1b 会话详情</div>
    </MScreen>
  );
}
