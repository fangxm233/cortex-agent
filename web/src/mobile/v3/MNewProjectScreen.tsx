// 1i 新建项目 — mobile v3 screen slot (scheme-mobile.dc.html). Placeholder body; filled by its own pass.
import { MScreen, MC } from '@/mobile/ui/kit';

export function MNewProjectScreen() {
  return (
    <MScreen label="1i 新建项目">
      <div style={{ padding: 16, color: MC.muted, fontSize: 13 }}>1i 新建项目</div>
    </MScreen>
  );
}
