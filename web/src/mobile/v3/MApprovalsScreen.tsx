// 1f 审批 — mobile v3 screen slot (scheme-mobile.dc.html). Placeholder body; filled by its own pass.
import { MScreen, MC } from '@/mobile/ui/kit';

export function MApprovalsScreen() {
  return (
    <MScreen label="1f 审批">
      <div style={{ padding: 16, color: MC.muted, fontSize: 13 }}>1f 审批</div>
    </MScreen>
  );
}
