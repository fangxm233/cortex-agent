// 1e 项目 — mobile v3 screen slot (scheme-mobile.dc.html). Placeholder body; filled by its own pass.
import { MScreen, MC } from '@/mobile/ui/kit';

export function MProjectScreen() {
  return (
    <MScreen label="1e 项目">
      <div style={{ padding: 16, color: MC.muted, fontSize: 13 }}>1e 项目</div>
    </MScreen>
  );
}
