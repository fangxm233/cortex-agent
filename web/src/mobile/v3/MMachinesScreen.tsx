// 1k 机器 — mobile v3 screen slot (scheme-mobile.dc.html). Placeholder body; filled by its own pass.
import { MScreen, MC } from '@/mobile/ui/kit';

export function MMachinesScreen() {
  return (
    <MScreen label="1k 机器">
      <div style={{ padding: 16, color: MC.muted, fontSize: 13 }}>1k 机器</div>
    </MScreen>
  );
}
