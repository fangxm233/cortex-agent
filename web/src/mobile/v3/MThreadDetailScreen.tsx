// 1g 线程详情 — mobile v3 screen slot (scheme-mobile.dc.html). Placeholder body; filled by its own pass.
import { MScreen, MC } from '@/mobile/ui/kit';

export function MThreadDetailScreen() {
  return (
    <MScreen label="1g 线程详情">
      <div style={{ padding: 16, color: MC.muted, fontSize: 13 }}>1g 线程详情</div>
    </MScreen>
  );
}
