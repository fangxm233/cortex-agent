# mobile/ — Cortex mobile v3 (scheme-mobile.dc.html)

The mobile client shell + the full v3 screen set. The SPA renders a **full-bleed, edge-to-edge**
viewport with a bottom four-Tab nav (**会话 / 线程 / 任务 / 项目**); the active screen swaps through
`<Outlet/>`. Desktop is a separate router (see `RootRouter`) and is byte-identical. **The OS draws its
own status bar, dynamic island, home indicator and screen corners — we do NOT paint a mock device
frame.** Screens reserve the OS chrome via `env(safe-area-inset-*)`: headers pad the top inset; the Tab
bar / composer / non-Tab bottom gutters pad the bottom inset. Design source (GROUND TRUTH):
`context/projects/cortex-self/design/ref/scheme-mobile.dc.html` (sections 1a–1r plus §2 composer
multi-line growth / full-screen editor). Raw px/hex/font by
design (§8.3, `@ds-adherence-ignore`) — the mobile palette is not in the light `proto.*` token set.

## v3 structure

The **project is the global scope**: 会话 / 线程 / 任务 only show the current project (a passive QN
scope tag in the header); the **项目 tab** shows project info + switches the current project. The 项目
tab carries an amber `需要你` count badge (pending approvals). Copy is per-screen and inline
(`const COPY = { en, zh }` + `pickCopy(useLang(), COPY)`) — the shared `i18n/vocab.ts` only holds the
tab label `project` (v3 screens deliberately avoid the vocab bottleneck).

| path | role |
|---|---|
| `mobile-tabs.ts` | **Pure** v3 Tab model: `MOBILE_TABS` (sessions/threads/tasks/**project** → `/m/*`), `activeTabId` (drill-in sub-routes map to their origin tab via `SUBROUTE_TAB`), `isTabRoute` (bottom bar shown only on the 4 tab routes), `tabBadge` (项目 amber 需要你 count). |
| `BottomTabBar.tsx` | Presentational bottom Tab bar (1a L121-126): 4 SVG icons (会话 chat / 线程 nodes / 任务 checks / 项目 folder), active `#191C22` / inactive `#98A1B0`, amber `#C99A2E` count badge on 项目, ≥44px touch, zh labels from `useVocab`. |
| `MobileShell.tsx` | Frame owner: full-bleed `100dvh` flex column + `BottomTabBar` (only on Tab routes) + the animated `<AnimatedOutlet/>` slot, wrapped in `MobileProjectProvider`; mounts the global `MNotificationProvider` (1q) + `MHotUpdateProvider` (3a hot-update prompt). Binds real `approvals.list` → 需要你 badge. |
| `MobileAnimatedOutlet.tsx` | Route-transition wrapper replacing the bare `<Outlet/>`: horizontal slide on screen swaps (iOS push/pop) — PUSH slides the incoming screen in from the right (outgoing shifts left), POP reverses, REPLACE / same-path / `prefers-reduced-motion` / **switches between the 4 bottom-Tab screens** swap instantly (no slide — the Tab bar is a flat switch, only drill-in sub-screens slide). Freezes the outgoing route element (`useOutlet()` snapshot) so both layers render until the incoming animation ends. Uses the existing Tailwind `animate-slide-*` utilities — no new deps. Pure `planTransition` / `slideAnimClasses` are unit-tested (`.test.tsx`); the DOM enter/exit is proven in the live harness. |
| `current-project.tsx` | `MobileProjectProvider` / `useMobileProject` — the mobile-wide current-project state (reuses the desktop pure `resolveCurrentProjectId`). 会话/线程/任务 scope to it; 项目 (1e) switches it. |
| `mobile-routes.tsx` / `mobile-router.tsx` | Route table + router. 4 tab routes + drill-in sub-screens `/m/session/:id` (1b) · `/m/session/:id/plan/:requestId` (6b 计划阅读页) · `/m/thread/:id` (1g) · `/m/task/:id` (1h) · `/m/approvals` (1f) · `/m/issues` (24c) · `/m/new-project` (1i) · `/m/memory` (1j) · `/m/machines` (1k) · `/m/settings` (1l) · `/m/daemon` (1r). Index + catch-all → `/m/sessions`. |
| `ui/kit.tsx` | **Shared mobile UI kit** (chrome extracted 1:1 from the scheme): `MScreen`, `MTabHeader`, `MDrillHeader`, `MMoreButton`, `MScrollBody`, `MCard`, `MPill`/`statusPillTone`, `MDot`, `MGroupLabel`, `MSegmented`, `MBottomSheet`, `MComposer` (**growable multi-line composer**, scheme §2: a single line reads as the 46px pill, typing rows auto-grows the field radius 14→16 up to a 5-line cap then it scrolls internally — Enter inserts a newline, sending is the button only; while a turn runs Send swaps to a Stop button via `running`/`onStop`; at the 5-line cap a top-right Expand button opens…; `tone='amber'` renders the 5a reject-mode ring `#C99A2E` + amber glow; `tone='accent'` holds the 7b edit-mode run-blue ring + glow even unfocused), `ComposerFullscreen` (**2b 全屏编辑** — absolute overlay of the chat body region covering the transcript from below the header to above the keyboard; ＋ / slash / `N 行 · M 字` counter / Send collapse into a bottom tool row, a top-right button collapses back; pure helpers `composerLineCount` / `composerCharCount` / `composerCountLabel` are unit-tested), `MC` (palette), `MONO`. Every v3 screen composes these. |
| `ui/format.ts` | Pure formatters: `relTimeZh`, `fmtMoney`, `pickCopy`. |

## v3 screens (`v3/`)

Each screen = `M<X>Screen.tsx` (container: tRPC + state + nav) + `M<X>View.tsx` (pure presentational)
+ `m-<x>-vm.ts` (pure logic) + tests. All render REAL tRPC data with **honest placeholders** for fields
the contract does not carry (never fabricated; flagged `// GAP`).

| screen | files | data |
|---|---|---|
| 1a 会话列表 | `MSessionListScreen/View`, `m-session-list-vm` | `sessions.list`(direct, scoped) day-grouped; ＋→`/m/session/new`; row→chat. **Unread (parity with desktop LeftRail):** `SessionInfo.unread` floats unread sessions first inside each day group (reuses `groupSessions`) + marks them with an accent dot + full-ink semibold title (read rows soften to `#454C59`); cleared once the chat is opened (see 1b `useMarkSessionRead`). **Background-held rows** (`SessionInfo.backgroundRunning`, web bg-hold snapshot) show an amber pulse dot + `后台运行` status line instead of the run-blue `running` (survives app restarts — snapshot from `sessions.list`). |
| 1b 会话详情 (+1o/1p, 交互卡 4/5/6) | `MChatScreen`, `MChatView`, `m-chat-vm` | `sessions.transcript`+`sessions.send`/`createAndSend`+live; profile switch (`sessions.setProfile`), attachments upload. **Per-session draft persistence (parity with desktop):** the composer text + uploaded attachments persist to localStorage per scope via the shared `features/workbench/composer-draft` (new-session draft keyed per project incl. its `draftUploadId`; survives app + server restart — restored media re-fetch an auth preview via `lib/files`; cleared on send). **Inline thread card (1b L148-158):** `InlineThreadCard` renders below the transcript, bound to REAL `threads.get` but **scoped to THIS session** — `threads.list({status:['running','waiting'], sessionId})` resolves the session→channel server-side and returns only threads on it, so the card shows the thread this conversation spawned (never a random global one, the former "wrong time" bug); hidden on drafts (no `sessionId`) and when the session owns no active thread. `打开 →` drills to 1g. **Stop (running):** while the session is running the composer's Send button swaps for a Stop button (white square on ink, `MComposer running`/`onStop`) → `sessions.cancel({sessionId})` cancels the agent(s) on the channel (same path as the desktop Composer); `running` collapses to idle as the live stream quiets. **Unread write side:** mounts the desktop `useMarkSessionRead(sessionId, `liveTail.length:running`)` — viewing stamps the session read (debounced, visibility-gated, re-arming on live activity) → invalidates `sessions.list` so the 1a marker + 1e switcher badge clear. **Message edit + rewind (scheme sec-7, 7a/7b):** long-press (~450ms, or contextmenu) a **user** bubble → the 7a action overlay (conversation dims + blurs, held bubble floats, 复制 / 编辑消息 menu — 编辑 greyed while running); agent messages carry no long-press copy affordance; 编辑消息 loads the original text into the composer in **7b edit mode** — accent `EditBar`（编辑消息 — 发送将回退后续回复 + × 原样退出，恢复此前草稿文本）+ `MComposer tone='accent'`, the original bubble rings accent with the 编辑中 badge, later rows dim under the amber 将被回退 · N 条回复 · M 次工具调用 badge (`rewindStats`); send fires the real `sessions.rewind` mutation (auto-disarms if the anchored row reshapes or a turn starts). Edited bubbles hang a tappable 已编辑 note → 原消息 bottom sheet; the first regenerated reply carries the 由编辑重新生成 footnote (`regenNoteIndexes`, shared with desktop). **Interaction cards WIRED 1:1 (scheme 4/5/6, superseding the old 1m/1n cards):** transcript entity rows render `MInteractionCards` (5b 逐问推进 ask card · 6a 薄 plan 卡 · 4a/4b/4c 原地封存 + 4c feedback user bubble); the screen owns the session-local progressive answer state (`interaction-vm` reducer — the entity resolves ONCE via `sessions.answerQuestion` when the last question commits), 5a reject mode (`rejectBar` amber context bar + reason chips + amber `MComposer tone`, send = `rejectPlan(id, feedback)`, armed by the card's 驳回并反馈 or the 6b page's router state, auto-disarmed if the plan resolves elsewhere), composer answer routing (pending ask → typed text answers the CURRENT question, placeholder `点选项，或直接输入回答 Q{k}…`), and the amber waiting header (`interactionHeaderStatus` → `计划待批/等待你的回答 k/n · 线程已暂停`). **⋯ 菜单 → 会话ID (`SessionIdSheet`):** the header ⋯ `MoreMenu` now leads with a real **会话ID** item (above the inert 重命名/导出/归档) that opens a bottom sheet showing the two identifiers — Cortex ID (cortex-XXXX, `SessionInfo.name`) + 后端 UUID (`SessionInfo.backendSessionId` — the backend CLI resume target, NOT the track `sessionId`) — each copy-to-clipboard, built from the shared `features/workbench/session-id` `buildSessionIdRows`. |
| 6b 计划阅读页 | `MPlanReadScreen`, `MPlanReadView` (+ shared `features/workbench/plan-read-vm`) | Route `/m/session/:sessionId/plan/:requestId` (drilled from the 6a file row / sealed 查看完整计划 ›). Data = `findInteraction` over the REAL `sessions.transcript` (the plan snapshot is persisted on the entity). Header title + `path · N 行 · 状态` + pill + scroll-progress bar; `ChatMarkdown` body; resident action bar — 批准并执行 (sub `已读 N% · 下滑读完或直接批准` until read to the bottom) + 驳回并反馈 (routes back to the chat armed in 5a via router state); sealed → read-only status stamp. Live-converges via `useSessionMessageLiveSync`. GAP: `由 X 生成` source line has no entity field → only the real row time. |
| 交互卡组件 | `MInteractionCards` (+ test) | **Presentational 1:1 cards** (scheme sec-6 6a · sec-5 5b/5a-dim · sec-4 4a/4b/4c): `MAskCard` (progress pill `Agent 提问 · k/n`, TTL `MM:SS 后按默认继续` from the shared `useInteractionTtl`, sealed green rows / expanded current question with 默认 badge + real option-description meta / dashed queued rows, multi-select ☑+确认 — flagged 13b-semantics completion, 自定义… → composer) and `MPlanCard` (thin 6a card with the file row as main entry; 5a dimmed collapse; 4b/4c seals + feedback bubble). zh/en copy `M_INT_COPY`. GAP: `来自 X` footer source omitted (no entity field). |
| 1c 线程 | `MThreadsScreen/View`, `m-threads-vm` | `threads.list`(scoped)+`threads.get`+`cost.summary` budget band; 活跃/历史; row→`/m/thread/:id` |
| 1d 任务 | `MTasksScreen/View`, `m-tasks-vm` | `tasks.list`(scoped)+live; 可执行 segment = 进行中/可执行; 全部 segment adds 阻塞 + 完成 (done group, desktop parity) and its count includes done; task text is single-line ellipsis-truncated; row→`/m/task/:id` |
| 1e 项目 | `MProjectScreen/View`, `m-project-vm` | `projects.list`+`cost.summary`+`threads.list`+`approvals.list`+`machines.list`; switch project; →memory/machines/settings/approvals/new-project. **Most-recent-activity ordering (parity with desktop LeftRail):** an UNSCOPED `sessions.list`(direct) → `lastActivityByProject` (reused from `features/workbench/left-rail-projects`) drives `buildProjectSwitchRows` via the shared `sortProjectsByActivity`, so the project you touched last floats to the top; the order derives from the persistent session-registry `lastUsedAt` and survives server/app restarts. `unreadCountByProject` still badges each switch row with its unread count (accent pill) but no longer drives ordering. Kept fresh by `useSessionsLiveSync`. |
| 1f 审批 | `MApprovalsScreen/View`, `m-approvals-vm` | `approvals.list`+approve/reject; first card expanded |
| 24c Issues | `MIssuesScreen/View`, `m-issues-vm` | `issues.list({projectId})`+`issues.delete`/`issues.handle` (处理 = 新建会话携带 issue 全文 → drills into `/m/session/:id`); route `/m/issues`, drilled from the 项目 tab's Issues card (24a, hidden at 0); first card expanded即决策, NO amber (issues never block, never in 需要你); field labels verbatim from ISSUES.md (`m-issues-vm` reuses `features/issues` `parseIssueBody`) |
| 1g 线程详情 | `MThreadDetailScreen/View`, `m-thread-detail-vm` | `threads.get`+live; pipeline steps + artifacts + Σcost; `threads.cancel` |
| 1h 任务详情 | `MTaskDetailScreen/View`, `m-task-detail-vm` | `tasks.list`+`tasks.verification`; read-only; done-when/deps/history |
| 1i 新建项目 | `MNewProjectScreen/View`, `m-new-project-vm` | `projects.create` bottom sheet |
| 1j 项目记忆 | `MMemoryScreen/View`, `m-memory-vm` | `memory.tree`(scoped); read-only file/dir tree |
| 1k 机器 | `MMachinesScreen/View`, `m-machines-vm` | `machines.list` (reuses `screens/mobile-machines-vm`) |
| 1l 设置 | `MSettingsScreen/View`, `m-settings-vm` | `config.get`+`cost.summary`; budget; desktop-only editing marked 桌面编辑 |
| 1r Daemon | `MDaemonScreen/View`, `m-daemon-vm` | `system.daemonStatus`+`threads.list`+`schedules.list`+`executions.list`+`system.restart` |
| 1q 通知 | `MNotificationProvider`, `MNotificationToaster` | **native OS/system notifications** over real `session.message`/`system.notice` via `features/notifications/os-notify` (Tauri notification plugin); reuses desktop notification store/vm/hooks. **Tap → deep-link**: the `{sessionId, projectId}` payload rides in the notification `extra`; `onOsNotificationAction` reads it back and re-points project + drills into `/m/session/:id` (design 1q "轻点直达对应会话"). Falls back to the in-app banner (`MNotificationToaster`, same tap-to-open) only when the OS path can't deliver (plain browser / permission denied). |
| 3a 热更新 | `MHotUpdateProvider`, `MHotUpdateDialog` | **热更新弹窗** — centered full-width alert raised when the shell stages a newer SPA bundle (OTA). Reuses the shared `features/hot-update` layer (seam + `useHotUpdate`); presentation is 1:1 scheme-mobile 3a (46px run-blue icon square + 「新版本已就绪」 + Plex Mono hash version line + 满宽 ink「退出 App」 + 文字「忽略」). 「退出 App」→ Android `app.exit(0)` (system relaunch applies). Mounted in `MobileShell`; APP-only (off-shell no-op). |

## Notes

- **Image/video lightbox** (`features/media`, shared with desktop): `MediaViewerProvider` is mounted in
  `MobileShell`; the composer chips (`MChatView` `ComposerChip`, local `URL.createObjectURL` preview)
  and stream attachment tiles (`AttachmentTile`, auth-fetched thumbnail — user AND agent media) open the
  full-screen **modal** previewer via `useMediaViewer().openMedia` instead of downloading / a new tab.
  Plain files keep the download tap.
- **Render switch**: `src/RootRouter.tsx` mounts `mobileRouter` (mobile) or the desktop `router`.
- **Live data / no backend change**: every screen uses the existing `ui-service` tRPC contract; no
  agent-server change was needed for v3 (the contract already covered it).
- **`screens/` is legacy (v2, 5a–5c/10e/10f)** — superseded by `v3/` and no longer routed. It is kept
  because a few of its **pure** helpers are still reused by v3 (`mobile-session-vm` stepper/tool-chips,
  `MobileThreadStepper`, `mobile-machines-vm`); its container components are dead (tree-shaken) and can
  be removed in a later cleanup once those helpers are relocated.
- `IOSDevice.tsx` is a design-preview specimen only (NOT used by the live shell).
- Verified: `pnpm`/web `tsc --noEmit` EXIT 0; web `vitest src/mobile` all green; `vite build` EXIT 0.
