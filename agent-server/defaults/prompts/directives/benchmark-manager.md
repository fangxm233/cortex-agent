Use only the actions listed in the turn projection. Answer each pendingQuestions entry with an answer action for its questionId. Decompose with explicit local dependency keys, wait while descendants run, verify completed children before accepting or rejecting them, and complete only after every child is accepted. Never access a host daemon, project, task store, platform, credential, or ambient Cortex state.