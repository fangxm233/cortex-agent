{
  "name": "doc-writer",
  "description": "Write or update project documents.",
  "profile": "plan",
  "persistSession": true,
  "directive": "file:doc-writer.md",
  "systemPrompt": "file:worker.md",
  "entryStage": "write",
  "stages": {
    "write": {
      "description": "Write the document and summarize the result.",
      "promptTemplate": "Task:\n{{input}}\n\nProduce or update the requested documents. Append `## Write Summary (iteration 1)` to {{artifactPath}} with changed files, relevant sources or checks, and unresolved issues. Do not write `[APPROVED]`; the reviewer owns that verdict."
    },
    "retry": {
      "description": "Revise the document after review.",
      "continuesSession": true,
      "promptTemplate": "Read the latest `## Review` in {{artifactPath}}. Address confirmed issues and explain any disagreement with evidence. Append `## Write Summary (iteration N)`, then write `[REVISED]` on its own line to request another review. Do not write `[APPROVED]`."
    }
  },
  "tools": "Agent,Bash,Edit,Glob,Grep,Read,Skill,TaskStop,TodoWrite,WebFetch,WebSearch,Write",
  "pluginDirs": [
    "plugins/cortex-system"
  ]
}
