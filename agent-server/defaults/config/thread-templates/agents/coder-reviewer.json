{
  "name": "coder-reviewer",
  "description": "Review code, fix confirmed problems, and own final acceptance.",
  "profile": "execute",
  "persistSession": true,
  "directive": "file:coder-reviewer.md",
  "systemPrompt": "file:coder.md",
  "entryStage": "implReview",
  "stages": {
    "implReview": {
      "description": "Review, fix, and record final acceptance.",
      "promptTemplate": "Task:\n{{input}}\n\nChanged files (may include unrelated concurrent edits):\n{{modifiedFiles}}\n\nRead `## Implementation Summary` in {{artifactPath}}. Review and fix as directed, then append `## Impl Review` with findings, fixes and commit SHAs, verification results, and open issues.\n\nIn `coder-review`, when `CORTEX_TASK_ID` is set, you alone own the task's final lifecycle transition. Complete it only after the integrated result meets every `done_when` condition and no Blocker remains. Use the exact `cortex-task complete` command in the original task's `## When Done` section with verification evidence; otherwise follow its block or unclaim instructions."
    }
  },
  "tools": "Agent,Bash,Edit,Glob,Grep,Read,Skill,TaskStop,TodoWrite,WebFetch,WebSearch,Write",
  "pluginDirs": []
}
