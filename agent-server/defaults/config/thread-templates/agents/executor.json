{
  "name": "executor",
  "description": "Execute a scoped task and verify the result.",
  "profile": "execute",
  "persistSession": true,
  "directive": "file:executor.md",
  "systemPrompt": "file:worker.md",
  "entryStage": "execute",
  "stages": {
    "execute": {
      "description": "Execute and summarize the result.",
      "promptTemplate": "Task:\n{{input}}\n\nExecute the task. Append `## Execute Summary (iteration 1)` to {{artifactPath}} with changes, verification results, and unresolved issues. Do not write `[APPROVED]`; the reviewer owns that verdict."
    },
    "retry": {
      "description": "Address issues found in review.",
      "continuesSession": true,
      "promptTemplate": "Read the latest `## Review` in {{artifactPath}}. Address confirmed issues and explain any disagreement with evidence. Append `## Execute Summary (iteration N)`, then write `[REVISED]` on its own line to request another review. Do not write `[APPROVED]`."
    }
  },
  "tools": "Agent,Bash,Edit,Glob,Grep,Read,Skill,TaskStop,Write,WebFetch,WebSearch",
  "pluginDirs": [
    "plugins/cortex-system"
  ]
}
