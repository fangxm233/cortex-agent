{
  "name": "executor-reviewer",
  "description": "Review execution results without performing the fixes.",
  "profile": "execute",
  "persistSession": true,
  "directive": "file:executor-reviewer.md",
  "systemPrompt": "file:worker.md",
  "promptTemplate": "Task:\n{{input}}\n\nChanged files (may include unrelated concurrent edits):\n{{modifiedFiles}}\n\nRead `## Execute Summary` in {{artifactPath}} and inspect the actual results. Append `## Review (iteration N)` with evidenced issues, their severity, and suggested corrections. Write `[APPROVED]` on its own line only if no Blockers remain; otherwise list the Blockers for revision.",
  "tools": "Agent,Bash,Edit,Glob,Grep,Read,Skill,TaskStop,Write,WebFetch,WebSearch",
  "pluginDirs": [
    "plugins/cortex-system"
  ]
}
