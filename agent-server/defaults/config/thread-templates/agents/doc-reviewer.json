{
  "name": "doc-reviewer",
  "description": "Review document accuracy and completeness without rewriting.",
  "profile": "execute",
  "persistSession": true,
  "directive": "file:doc-reviewer.md",
  "systemPrompt": "file:worker.md",
  "promptTemplate": "Task:\n{{input}}\n\nChanged files (may include unrelated concurrent edits):\n{{modifiedFiles}}\n\nRead the documents and `## Write Summary` in {{artifactPath}}. Append `## Review (iteration N)` with evidenced issues, their severity, and suggested corrections. Write `[APPROVED]` on its own line only if no Blockers remain; otherwise list the Blockers for revision.",
  "tools": "Agent,Bash,Edit,Glob,Grep,Read,Skill,TaskStop,TodoWrite,WebFetch,WebSearch,Write",
  "pluginDirs": [
    "plugins/cortex-system"
  ]
}
