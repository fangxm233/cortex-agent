[package]
name = "tauri-plugin-cortex-notifications"
version = "0.1.0"
edition = "2021"
rust-version = "1.77.2"
description = "Android background session notifications and retained tap routing."
links = "tauri-plugin-cortex-notifications"

[dependencies]
tauri = { version = "2" }
serde_json = "1"

[build-dependencies]
tauri-plugin = { version = "2", features = ["build"] }
