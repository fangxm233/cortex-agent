[default]
description = "Post notifications, report the visible session and consume retained taps."
permissions = [
  "allow-post", "allow-visible-session", "allow-pending-actions", "allow-ack-action",
  "allow-register-listener", "allow-remove-listener"
]
